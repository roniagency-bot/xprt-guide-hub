Update team section title and related CTAs to "Meet the XPRTs".

## Changes
1. `src/components/ui/kinetic-team-hybrid.tsx` — update `<motion.header>` heading from "People you can call directly." to "Meet the XPRTs"
2. `src/routes/about.tsx` — update hero CTA button from "Meet the team" to "Meet the XPRTs"

Keep the eyebrow as "The team" and the muted secondary line can be removed or simplified since the new title is concise.